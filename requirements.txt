# AI 资讯追踪·生产依赖（v2.3.3 新增）
# 用途：calendar 沙箱任务（python3 -m scripts.cli <sub>）启动前自愈安装
# 安装命令：pip3 install --user -r requirements.txt
# 设计：与 pyproject.toml [project].dependencies 保持一致，calendar 走 python3 -m scripts.cli 时用本文件兜底

# HTTP / 反爬
requests>=2.33
cloudscraper>=1.2

# 解析
feedparser>=6.0
trafilatura>=2.0
lxml>=5.0  # trafilatura 强依赖

# 配置 / 数据
PyYAML>=6.0

# LLM
openai>=1.40
